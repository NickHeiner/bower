{
    "command": "mirror",
    "usage": [
        "mirror <package-name> <dest-repo>",
        "mirror <package-name>#<version> <dest-repo>"
    ],
    "options": [],
    "description": "Mirrors a repo from where it lives currently to the specified <dest-repo>. You must have push permissions to that repo."
}
